-- 코드를 입력하세요
SELECT F.USER_ID, F.NICKNAME, SUM(B.PRICE) AS TOTAL_SALES
FROM USED_GOODS_BOARD B JOIN USED_GOODS_USER F ON B.WRITER_ID = F.USER_ID
WHERE B.STATUS = 'DONE'
GROUP BY F.USER_ID
HAVING SUM(PRICE) >= 700000
ORDER BY 3